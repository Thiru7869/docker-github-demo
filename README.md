# Docker GitHub Demo

This project demonstrates how to run a GitHub project using Docker.

## Steps to Run

1. Clone the repository
   git clone https://github.com/your-username/docker-github-demo.git

2. Go inside the project folder
   cd docker-github-demo

3. Build Docker image
   docker build -t docker-demo .

4. Run Docker container
   docker run docker-demo
